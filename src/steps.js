/*
 *steps to use firebase
 *create a project on console.fiebase.google.com
 *install firebase
 *Register web app in firebase
 *coppy firebase int with config from firebase project settings
 *export default app
 *import app firebase init.s into your app.js
 *import getAuth from firebase/auth and create auth = getAuth(app)
 *turn on google authentication (firebase >authentication> enable google sign in)
 *create google provider
 *use signInWithPopup and pass auth and provider
 *handle .then (if successful) and catch error (if error)
 */
